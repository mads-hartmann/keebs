- Feature(key mappings): Add support for defining key mappings where a key sequence can be mapped to another key sequence, e.g. ⌃n to ↓. This should run before Shift Lock so that ⌃<space> ⌃n is the same as ⌃<space> ↓. As part of this the README should be extended with a diagram showing the layering of key-mappings. This replaces my use of karabiner elements for basic mappings 
- Feature(hyper key + chords): Add support for key chords and have those open applications via passing args to "open". This allows me to replace my karabiner setup for hyper key. You should be able to hit the "Right ⌘" key (my hyper key) which should then open a HUD with all the keys in the top layer (e.g) "r" for "raycast" and "a" for applicaitons. If I hit "r" then the options should be "w,c,k,s,i,n" and "a" should be "t,e" to match my current karabiner elmeents setup. Not now it's fine if only support 2 layers and the exact mappins can be hardcoded in the source code as we're not trying to make this usable for anyone else. In the future we could introduce a config format of course.
- Tweak(menu bar app): Turn the app into a Menu bar app so we have a way to add a "launch at startup" setting as well as a way to quit it, view logs, etc.
- Tweak(translation info): The Shift Lock HUD should show briefly the keyboard keys that it processes and what it translates them to, similar to how screen capture programs do it. This provides a bit of extra feedback that the shift lock feature is working as intended for the user.
